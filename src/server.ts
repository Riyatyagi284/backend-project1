// import console from "node:console";

export const addTwoNumsFunction = (a: number, b: number): Number => {
    return a + b;
};

// console.log(`2 + 4 = ${addTwoNumsFunction(2, 4)}`);
// console.log(`2 + 3 = ${addTwoNumsFunction(2, 3)}`);